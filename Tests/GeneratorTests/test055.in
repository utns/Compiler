type 
    point = record
        x, y: integer;
    end;
    
procedure foo(var p: point);
begin
    p.x := 3;
    p.y := 4;
    writeln(p.x, ' ', p.y);
end;

var
    p: point;
begin
    p.x := 1;
    p.y := 2;
    foo(p);
    writeln(p.x, ' ', p.y);
end.